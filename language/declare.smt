// Simple variable declaration
let hello = "Hello, World!";

// Constant declaration (cannot be changed)
const pi = 3.14;

// Print the values to the console
writeln("Welcome message:", hello);
writeln("PI value is:", pi);
